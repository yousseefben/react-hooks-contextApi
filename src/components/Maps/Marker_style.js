const K_WIDTH = 80;
const K_HEIGHT = 80;

const markerStyle = {
  position: 'absolute',
  width: K_WIDTH,
  height: K_HEIGHT,
  left: -K_WIDTH / 2,
  top: -K_HEIGHT / 2,

  backgroundColor: '#3f51b5',
  opacity: 0.4,
  textAlign: 'center',
  border: '1px solid blue',
  borderRadius: K_HEIGHT,
  color: '#3f51b5',
  fontSize: 16,
  fontWeight: 'bold',
  padding: 4
};

export {markerStyle};
